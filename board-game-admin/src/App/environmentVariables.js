export default {
  apiIdentifier: API_IDENTIFIER.trim(),
  apiUrl: API_URL.trim(),
  authDomain: AUTH_DOMAIN.trim(),
  authClientId: AUTH_CLIENT_ID.trim(),
  authCallbackUrl: AUTH_CALLBACK.trim(),
  logoutReturnUrl: LOGOUT_RETURN_URL.trim()
};
